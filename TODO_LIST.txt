======================================
GAME CLASS REFACTORING - EX3 FOUNDATION
======================================

Goal: Transform Game class into an abstract base class with NormalGame and LoadedGame as concrete implementations.
This creates the foundation for the recording/replay system (EX3).

PHASE 1: Prepare Base Class
----------------------------
[✓] Make Game destructor virtual (~Game() → virtual ~Game())
[✓] Move Game constructor to protected section
[✓] Test compilation - should still work identically

PHASE 2: Create NormalGame Class
---------------------------------
[✓] Create NormalGame.h (class declaration inheriting from Game)
[✓] Create NormalGame.cpp (implement constructor and handleInput())
    - Extract current Game::handleInput() logic to NormalGame::handleInput()
[✓] Update main.cpp to use NormalGame instead of Game
[✓] Update Makefile to include NormalGame.cpp and NormalGame.h
[✓] Test compilation with NormalGame - game should work identically

PHASE 3: Make Game Abstract
----------------------------
[✓] Mark handleInput() as pure virtual in Game.h: virtual void handleInput() = 0;
[✓] Remove Game::handleInput() implementation from Game.cpp
[✓] Mark these methods as virtual (with default implementation):
    - virtual void gameLoop();
    - virtual void startNewGame();
    - virtual void update();
    - virtual void showMainMenu();
    - virtual void showInstructions();
    - virtual void showPauseMenu();
[✓] Test - should compile with NormalGame, Game cannot be instantiated directly

PHASE 4: Create LoadedGame Class
---------------------------------
[✓] Create LoadedGame.h (class declaration inheriting from Game)
[✓] Create LoadedGame.cpp with complete implementation:
    - Constructor: LoadedGame::LoadedGame() : Game() {}
    - handleInput(): Stub implementation (empty for now)
[✓] Update Makefile to include LoadedGame.cpp and LoadedGame.h
[✓] Test - should compile with both NormalGame and LoadedGame

PHASE 5: Final Testing
-----------------------
[✓] Run make clean && make - should compile without warnings
[✓] Test game functionality with NormalGame:
    - Main menu works
    - Gameplay works (both players, objects, riddles, etc.)
    - Pause menu works
    - Victory and game over work
[✓] Verify Game cannot be instantiated directly (abstract base class)
[ ] Optional: Fix sign-comparison warnings in Game.cpp (lines 375, 410, 446)

======================================
EX3 RECORDING/REPLAY SYSTEM
======================================

PHASE 6.1: Add Cycle Tracking to Game Class
--------------------------------------------
[✓] Add protected member variable to Game.h:
    - unsigned long cycleCount = 0;
[✓] Increment cycleCount in Game::update() method
[✓] Test compilation

PHASE 6.2: Create ActionRecord Structure
-----------------------------------------
[✓] Create ActionRecord.h with:
    - struct ActionRecord { unsigned long cycle; Action action; int playerIndex; }
    - Serialization methods: write() and read()
[✓] Create ActionRecord.cpp with serialization implementation
    - Constructor from PlayerKeyBinding (eliminates redundancy)
    - write(ostream&) for text format: CYCLE: X | PLAYER: Y | ACTION: Z
    - read(istream&) with boolean return (no exceptions)
    - Private helpers: jumpToFieldValue(), extractFieldValue()
[✓] Create RecordedSteps class for managing action vector with index tracking
[✓] Create ToAddToReadme.txt with format specification
[✓] Update Makefile to include ActionRecord.cpp and ActionRecord.h
[✓] Test compilation

PHASE 6.3: Implement Action Recording in NormalGame (IMMEDIATE FILE WRITING - CRASH-PROOF)
------------------------------------------------------------------------------------------
[✓] Add includes to NormalGame.h:
    - #include "Recorder.h", <fstream>
[✓] Add private members to NormalGame.h:
    - bool isRecording = false;
    - ofstream recordingFile;
[✓] Add public methods to NormalGame.h:
    - void enableRecording(const string& filename);
    - void disableRecording();
[✓] Add private method to NormalGame.h:
    - void recordAction(const PlayerKeyBinding& binding);
[✓] Add destructor to NormalGame.h:
    - ~NormalGame();
[✓] Modify NormalGame::handleInput() to record actions:
    - Call recordAction(keyBindings[i]) BEFORE performing action
    - Recording only happens if -save flag was used in main.cpp         ///////////// NOT TAKEN CARE OF YET /////////
[✓] Implement recordAction() in NormalGame.cpp:
    - Create ActionRecord from binding and cycleCount
    - Write immediately to recordingFile using record.write()
    - Call recordingFile.flush() after write (ensures disk write - crash-proof!)
[✓] Implement enableRecording() in NormalGame.cpp:
    - Open recordingFile with given filename
    - Set isRecording = true if successful
[✓] Implement disableRecording() in NormalGame.cpp:
    - Close recordingFile
    - Set isRecording = false
[✓] Implement ~NormalGame() destructor in NormalGame.cpp:
    - Call disableRecording() to ensure file is closed
[✓] Test compilation

IMPORTANT: Immediate file writing strategy
- Each action writes to file immediately (not buffered in memory)
- flush() after each write ensures data reaches disk
- If game crashes, all actions up to crash point are saved
- No memory buildup for long gameplay sessions

PHASE 6.4: Implement Action Replay in LoadedGame
-------------------------------------------------
[✓] Add includes to LoadedGame.h:
    - #include "ActionRecord.h", <vector>, <string>, <fstream>
[✓] Add private members to LoadedGame.h:
    - std::vector<ActionRecord> recordedActions;
    - size_t currentActionIndex = 0;
[✓] Add private method to LoadedGame.h:
    - bool loadActions(const std::string& filename);
[✓] Update LoadedGame constructor to accept filename parameter:
    - LoadedGame(const std::string& filename);
[✓] Add handleInput() override declaration to LoadedGame.h
[✓] Implement new constructor in LoadedGame.cpp (loads actions from file)
[✓] Implement loadActions() in LoadedGame.cpp (reads text file)
[✓] Implement handleInput() in LoadedGame.cpp (injects recorded actions at correct cycles)
[✓] Test compilation

PHASE 6.5: Create SilentLoadedGame Class
-----------------------------------------
[ ] Create SilentLoadedGame.h (inherits from LoadedGame)
[ ] Create SilentLoadedGame.cpp with:
    - Constructor that accepts filename
    - Override render() methods to do nothing (no-op)
    - Override gameLoop() to run at maximum speed (no delays)
[ ] Update Makefile to include SilentLoadedGame.cpp and SilentLoadedGame.h
[ ] Test compilation

PHASE 6.6: Command-Line Argument Parsing
-----------------------------------------
[ ] Update main.cpp to parse command-line arguments:
    - Parse -save, -load, -silent flags
    - Validate flag combinations (-silent only with -load)
[ ] Update main.cpp to instantiate appropriate Game subclass:
    - NormalGame for normal play (with optional recording if -save)
    - LoadedGame for replay (if -load)
    - SilentLoadedGame for fast replay (if -load -silent)
[ ] Add necessary includes to main.cpp (LoadedGame.h, SilentLoadedGame.h)
[ ] Test compilation

PHASE 6.7: Final EX3 Testing
-----------------------------
[ ] Test recording: ./game -save
    - Play through game
    - Verify adv-world.steps file is created
[ ] Test replay: ./game -load
    - Watch recorded game replay
    - Verify actions match original gameplay
[ ] Test silent replay: ./game -load -silent
    - Fast replay without display
    - Verify game completes successfully
[ ] Test invalid combinations: ./game -save -silent
    - Should warn that -silent is ignored
[ ] Test determinism:
    - Record same playthrough twice
    - Verify both produce identical results
[ ] Remove debugging shortcuts
[ ] Optional: Fix sign-comparison warnings in Game.cpp (lines 375, 410, 446)
